Given an int variable n that has already been declared and initialized to a positive value,
use a do...while loop to print a single line consisting of n asterisks.

Use no variables other than n.

do {
  System.out.print('*');
} while (--n > 0);
System.out.println();